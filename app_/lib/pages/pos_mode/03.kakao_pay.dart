import 'package:flutter/material.dart';

class KakaoPay_Page extends StatelessWidget {
  const KakaoPay_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Container( // KakaoPay QR-Code

            ),
            Container(  // move back btn

            )
          ],
        ),
      ),
    );
  }
}